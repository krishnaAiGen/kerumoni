import type { NextConfig } from "next";

// Product images are served from /public or provided as full URLs and rendered
// with next/image `unoptimized`, so no remote host allowlist is required.
const nextConfig: NextConfig = {};

export default nextConfig;
